model Person {
    fields {
        name Text?
    }

    operations {
        create createPerson() with (name) {
            @permission(expression: true)
        }
        update updatePerson(id) with (name) {
            @permission(expression: true)
        }
    }
}

api Web {
    models {
        Person
    }
}
