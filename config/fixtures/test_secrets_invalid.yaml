# secrets.0.name: Does not match pattern '^[A-Z][A-Z0-9_]*$'
# secrets.1: Additional property value is not allowed

secrets:
  - name: "not-valid-name"
  - name: SOME_OTHER_KEY
    value: "Value cannot be provided for secrets"
