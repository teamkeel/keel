# auth.providers.0: name is required
# auth.providers.1: name is required

auth:
  providers:
    - type: google
      clientId: abcdef

    - type: oidc
      clientId: abcdef
      issuerUrl: https://some.url
