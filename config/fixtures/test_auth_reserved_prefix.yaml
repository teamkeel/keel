# auth.providers.0.name: Cannot start with 'keel_'
# auth.providers.1.name: Cannot start with 'KEEL_'

auth:
  providers:
    - type: google
      name: keel_client
      clientId: foo_1

    - type: google
      name: KEEL_CLIENT
      clientId: foo_1
