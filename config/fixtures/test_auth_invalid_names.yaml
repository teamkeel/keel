# auth.providers.0.name: Does not match pattern '^[a-zA-Z][a-zA-Z0-9_]+$'
# auth.providers.1.name: Does not match pattern '^[a-zA-Z][a-zA-Z0-9_]+$'
# auth.providers.2.name: Does not match pattern '^[a-zA-Z][a-zA-Z0-9_]+$'
# auth.providers.3.name: Does not match pattern '^[a-zA-Z][a-zA-Z0-9_]+$'
# auth.providers.4.name: Does not match pattern '^[a-zA-Z][a-zA-Z0-9_]+$'
# auth.providers.5.name: Does not match pattern '^[a-zA-Z][a-zA-Z0-9_]+$'
# auth.providers.6.name: Cannot start with 'keel_'

auth:
  providers:
    - type: google
      name: 12 34
      clientId: foo_1

    - type: google
      name: Google Client
      clientId: foo_2

    - type: google
      name: google\client
      clientId: foo_2

    - type: oidc
      name: google/
      authorizationUrl: "https://dev-skhlutl45lbqkvhv.us.auth0.com"
      issuerUrl: "https://dev-skhlutl45lbqkvhv.us.auth0.com"
      tokenUrl: "https://dev-skhlutl45lbqkvhv.us.auth0.com"
      clientId: "kasj28fnq09ak"

    - type: google
      name: 1google
      clientId: foo_1

    - type: google
      name: Google-Client
      clientId: foo_2

    - type: google
      name: keel_google
      clientId: foo_2
