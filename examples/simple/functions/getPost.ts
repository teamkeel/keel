import { GetPost } from '@teamkeel/sdk'

export default GetPost(async (inputs, api) => {
  // Build something awesome

  return {
    title: 'djjd',
    id: '8383',
    createdAt: new Date(),
    updatedAt: new Date()
  }
})