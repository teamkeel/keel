model Post {
    fields {
        title Text
    }

    operations {
        get getPost(id)
        create createPost() with (title)
        list listPosts(title)
    }
}

api Web {
    @graphql

    models {
        Post
    }
}
