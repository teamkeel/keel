model Person {
    fields {
      name Text
    }
}

===

model Person {
    fields {
        name Text
    }
}

model Animal {
    fields {
        name Text
        humanFriend Person
    }
}

===

CREATE TABLE "animal" (
"name" TEXT NOT NULL,
"id" TEXT NOT NULL,
"created_at" TIMESTAMP NOT NULL,
"updated_at" TIMESTAMP NOT NULL,
"human_friend_id" TEXT
);
ALTER TABLE "animal" ADD CONSTRAINT animal_id_pkey PRIMARY KEY ("id");

ALTER TABLE "animal" ADD FOREIGN KEY ("human_friend_id") REFERENCES "person"("id");

=== 

[
  { "Model": "Animal", "Field": "", "Type": "ADDED" }
]
