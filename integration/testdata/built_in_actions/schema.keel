model Post {
    fields {
        title Text
    }

    operations {
        create createPost() with (title)
        get getPost(id)
        update updatePost(id) with (title)
        delete deletePost(id)
        list listPosts(title)
    }
}

api Web {
    @graphql

    models {
        Post
    }
}
