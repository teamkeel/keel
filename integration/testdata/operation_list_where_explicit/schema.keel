model Post {
    fields {
        title Text?
        length Number?
        aTimestamp Timestamp?
    }

    operations {
        create createPost() with (title?, length?, aTimestamp?)
        list listPostsEqualString(requiredTitle: Text) {
            @where(post.title == requiredTitle)
        }
        list listPostsNotEqualString(requiredTitle: Text) {
            @where(post.title != requiredTitle)
        }
    }
}

enum Category {
    Technical
    Lifestyle
    Food
}

api Web {
    @graphql

    models {
        Post
    }
}
