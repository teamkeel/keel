import { SendVerificationEmail } from "@teamkeel/sdk";

// To learn more about events and subscribers, visit https://docs.keel.so/events
export default SendVerificationEmail(async (ctx, event) => {});
