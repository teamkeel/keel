task EmptyFlow {
    fields {
        orderDate Date
        shipByDate Date
    }

    @permission(roles: [User])
    @orderBy(shipByDate: asc, orderDate: asc)
}

role User {
    domains {
        "keel.xyz"
    }
}
