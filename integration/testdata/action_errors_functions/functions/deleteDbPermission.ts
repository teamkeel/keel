import { DeleteDbPermission, DeleteDbPermissionHooks } from '@teamkeel/sdk';

// To learn more about what you can do with hooks,
// visit https://docs.keel.so/functions
const hooks : DeleteDbPermissionHooks = {};

export default DeleteDbPermission(hooks);
	