import { ListNotPermitted, ListNotPermittedHooks } from '@teamkeel/sdk';

// To learn more about what you can do with hooks,
// visit https://docs.keel.so/functions
const hooks : ListNotPermittedHooks = {};

export default ListNotPermitted(hooks);
	