import { UpdateNotPermitted, UpdateNotPermittedHooks } from '@teamkeel/sdk';

// To learn more about what you can do with hooks,
// visit https://docs.keel.so/functions
const hooks : UpdateNotPermittedHooks = {};

export default UpdateNotPermitted(hooks);
	