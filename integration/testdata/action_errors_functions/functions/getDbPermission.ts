import { GetDbPermission, GetDbPermissionHooks } from '@teamkeel/sdk';

// To learn more about what you can do with hooks,
// visit https://docs.keel.so/functions
const hooks : GetDbPermissionHooks = {};

export default GetDbPermission(hooks);
	