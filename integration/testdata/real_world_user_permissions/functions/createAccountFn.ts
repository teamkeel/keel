import { CreateAccountFn, CreateAccountFnHooks } from "@teamkeel/sdk";

// To learn more about what you can do with hooks, visit https://docs.keel.so/functions
const hooks: CreateAccountFnHooks = {};

export default CreateAccountFn(hooks);
