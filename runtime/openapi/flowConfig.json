{
  "type": "object",
  "properties": {
    "stages": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "key": {
            "description": "The unique key of the stage.",
            "type": "string"
          },
          "name": {
            "description": "The name of the stage as shown in the Console.",
            "type": "string"
          },
          "description": {
            "description": "The description of the stage as shown in the Console.",
            "type": "string"
          },
          "initiallyHidden": {
            "description": "Whether the stage is initially hidden in the Console.",
            "type": "boolean"
          }
        },
        "additionalProperties": false,
        "required": [
          "key",
          "name"
        ]
      }
    },
    "title": {
      "type": "string"
    },
    "description": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "title"
  ],
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}