model Customer {
    fields {
        name Text
        dateOfBirth Date
    }

    operations {
        get getCustomer(id)
        create createCustomer() with (name, dateOfBirth)
        update updateCustomer(id) with (name?, dateOfBirth?)
        list customers(name?)
    }
}

api Web {
    models {
        Customer
    }
}
