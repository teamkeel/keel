model Address {
    fields {
        addressLine1 Text
        town Text
    }
}

model Customer {
    fields {
        name Text
        dateOfBirth Date
        address Address
        details Markdown
        weight Decimal
    }

    actions {
        get getCustomer(id)
        create createCustomer() with (
            name,
            dateOfBirth,
            address.addressLine1,
            address.town,
            details,
            weight,
        )
        update updateCustomer(id) with (name?, dateOfBirth?, details?, weight?)
        list customers(name?)
    }
}

api Web {
    models {
        Customer
    }
}
