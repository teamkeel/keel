model Order {
    fields {
        items Item[]
        promoCode Text
    }
    operations {
        list listOrders(items.price, code:     Text) {
            @where(order.promoCode == code)
        }
    }
}

model Item {
    fields {
        price Number
    }
}

===

model Order {
    fields {
        items Item[]
        promoCode Text
    }

    operations {
        list listOrders(items.price, code: Text) {
            @where(order.promoCode == code)
        }
    }
}

model Item {
    fields {
        price Number
    }
}