model Profile {
    fields {
        identity Identity @unique
        username Text @unique
    }

    actions {
        update updateProfile(id) with (username) {
            //expect-error:20:51:AttributeExpressionError:Syntax error: token recognition error at: '= '
            @where(profile.identity = ctx.identity)
        }
    }
}
