model Profile {
    fields {
        identity Identity @unique
        username Text @unique
    }

    actions {
        get getProfile(username) {
            //expect-error:20:51:AttributeExpressionError:field 'identit' does not exist
            @where(profile.identit == ctx.identity)
        }
    }
}
