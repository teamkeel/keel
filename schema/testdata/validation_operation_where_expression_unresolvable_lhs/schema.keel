model Profile {
    fields {
        identity Identity {
            @unique
        }
        username Text {
            @unique
        }
    }

    actions {
        get getProfile(username) {
            @where(profile.identit == ctx.identity)
        }
    }
}
