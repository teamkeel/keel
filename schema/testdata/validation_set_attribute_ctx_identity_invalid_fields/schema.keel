model UserExtension {
    fields {
        unknown Text
    }

    actions {
        create createExt() {
            @set(userExtension.unknown = ctx.identity.unknown)
        }
        update updateExt(id) {
            @set(userExtension.unknown = ctx.identity.unknown)
        }
    }
}