model CompanyEmployee {
    fields {
        abcIdentity Identity {
            @unique
            @relation(abcEmployee)
        }
        xyzIdentity Identity {
            @unique
            @relation(xyzEmployee)
        }
    }
}
