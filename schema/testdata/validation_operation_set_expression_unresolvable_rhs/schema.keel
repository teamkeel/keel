model Profile {
    fields {
        username Text {
            @unique
        }
    }

    actions {
        create createProfile() {
            @set(profile.username = context.identity)
        }
    }
}
