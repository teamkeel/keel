model Profile {
    fields {
        email Text {
            @unique
        }
        username Text {
            @unique
        }
    }

    actions {
        create createProfile() with (username, email) {
            @save(profile.identity = ctx.identity)
        }
    }
}