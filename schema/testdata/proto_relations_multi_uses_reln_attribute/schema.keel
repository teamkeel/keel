model Account {
    fields {
        authoredPosts Post[]
        reviewedPosts Post[]
    }
}

model Post {
    fields {
        author Account @relation(authoredPosts)
        reviewer Account @relation(reviewedPosts)
    }
}
