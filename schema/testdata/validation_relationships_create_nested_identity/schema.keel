model Todo {
    fields {
        name Text
        owner Identity
    }

    operations {
        // this should trigger - "You cannot create models of type Identity during nested-creation of relationships"
        create createTodo() with (name)
    }
}
