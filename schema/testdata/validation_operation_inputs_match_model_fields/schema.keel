model Person {
  fields {
    profile Profile
  }

  operations {
    create createPerson(blah) {
      @set(person.profile.identity = ctx.identity)
    }
  }
}

model Profile {
  fields {
    identity Identity
  }
}
