
model BankAccount {
    fields {
        identity Identity @unique @relation(mainAccount)
    }
    actions {
        get getBankAccount(id) {
            @where(bankAccount == ctx.identity.mainAccount)
        }
    }
}