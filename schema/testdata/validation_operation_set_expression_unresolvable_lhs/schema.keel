model Profile {
    fields {
        username Text {
            @unique
        }
    }

    actions {
        update createProfile(id) with (username) {
            @set(profile.identit = ctx.identity)
        }
    }
}
