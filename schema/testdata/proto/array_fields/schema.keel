model Thing {
    fields {
        texts Text[]
        numbers Number[]
        booleans Boolean[]
        dates Date[]
        timestamps Timestamp[]
    }

    actions {
        create createThing() with (texts, numbers, booleans, dates, timestamps)
        list listThings(texts, numbers, booleans, dates, timestamps)
    }
}