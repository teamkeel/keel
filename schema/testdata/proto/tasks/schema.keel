task NewOrder {
    fields {
        ticketNo Text @sequence("S")
        order Order
        orderCode Text @computed(newOrder.order.orderCode)
        customerName Text
        customerCode Text @unique
        deliveryAddress Text
        notes Text @default("none")
        dueDate Date?
    }
    @unique([orderCode, customerCode])
}

model Order {
    fields {
        isExpedited Boolean
        orderCode Text
    }
}