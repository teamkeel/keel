model Profile {
    fields {
        identity Identity {
            @unique
        }
        username Text {
            @unique
        }
    }

    operations {
        update createProfile(id) with (username) {
            @set(profile.identity == ctx.identity)
        }
    }
}
