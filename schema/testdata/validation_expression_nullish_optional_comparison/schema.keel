model Post {
    fields {
        identity Identity?
    }

    operations {
        update updatePost(id) with (identity) {
            @permission(expression: post.identity == null)
        }
    }
}
