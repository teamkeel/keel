model Post {
    fields {
        identity Identity?
    }

    operations {
        create createPost() with (identity) {
            @permission(expression: post.identity == null)
        }
    }
}
