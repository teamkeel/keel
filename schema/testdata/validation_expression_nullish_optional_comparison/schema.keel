model Post {
    fields {
        identity Identity?
    }

    operations {
        create createPost() {
            @set(post.identity = ctx.identity)
            @permission(expression: post.identity == null)
        }
    }
}
