model User {
    fields {
        student Student {
            @unique
            @relation(createdAt)
        }
        student2 Student {
            @unique
            @relation(user2)
        }
    }
}

model Student {
    fields {
        user User
        user2 User
    }
}
