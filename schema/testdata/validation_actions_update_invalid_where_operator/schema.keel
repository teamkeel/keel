model Person {
    fields {
        name Text
        niNumber Number @unique
    }
    operations {
        update updateName() with (name) {
            @where(person.niNumber > 100)
        }
    }
}