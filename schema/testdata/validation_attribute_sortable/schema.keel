
model Author {
    fields {
        firstName Text
        surname Text
    }

    operations {
        create createAuthor() with (firstName, surname) {
            @sortable(firstName, surname)
        }

        list listAuthors() {
            @sortable(firstName)
            @sortable(surname)
        }
    }

    functions {
        list listAuthorsFn() {
            @sortable(firstName, surname)
        }
    }
}