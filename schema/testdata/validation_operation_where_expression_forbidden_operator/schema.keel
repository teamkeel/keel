model Profile {
    fields {
        identity Identity {
            @unique
        }
        username Text {
            @unique
        }
    }

    actions {
        update updateProfile(id) with (username) {
            @where(profile.identity = ctx.identity)
        }
    }
}
