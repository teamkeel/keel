model Profile {
    fields {
        identity Identity {
            @unique
        }
        username Text {
            @unique
        }
    }

    operations {
        create createProfile(username) {
            @where(profile.identity = ctx.identity)
        }
    }
}
