model Profile {
    fields {
        identity Identity {
            @unique
        }
        username Text {
            @unique
        }
    }

    operations {
        get getProfile(username) {
            @where(profile.identity = context.identity)
        }
    }
}
