model Profile {
  fields {
      identity Identity {
          @unique
      }
      username Text {
          @unique
      }
  }

  operations {
    create CREATE_PROFILE() with (username) {
        @set(profile.identity = ctx.identity)
    }
  }
}
