model Profile {
    fields {
        identity Identity {
            @unique
        }
        username Text {
            @unique
        }
    }

    actions {
        update createProfile(id) with(username) {
            @set(profile.identity)
        }
    }
}
