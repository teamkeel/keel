model MyModel {
  actions {
    get uppercase(UPPERCASE: ID) {
      @where(myModel.id == UPPERCASE)
    }
    get upperCamelCase(UpperCamelCase: ID) {
      @where(myModel.id == UpperCamelCase)
    }
    get snakeCase(snake_case: ID) {
      @where(myModel.id == snake_case)
    }
    get screamingSnakeCase(SCREAMING_SNAKE_CASE: ID) {
      @where(myModel.id == SCREAMING_SNAKE_CASE)
    }
  }

  actions {
    get uppercaseFunction(UPPERCASE: ID)
    get upperCamelCaseFunction(UpperCamelCase: ID)
    get snakeCaseFunction(snake_case: ID)
    get screamingSnakeCaseFunction(SCREAMING_SNAKE_CASE: ID)
  }
}
