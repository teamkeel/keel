model Profile {
    @permission(
      expression: profile.identity == ctx.identity,
      actions: [get, update]
    )
}

model Something {
  @permission(
    expression: something.identity == ctx.identity,
    actions: [get, update]
  )
}
