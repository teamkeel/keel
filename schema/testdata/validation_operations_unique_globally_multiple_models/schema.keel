model Profile {
    fields {
        status Text
    }

    operations {
        update updateStatus(id) with (status)
    }
}

model Order {
    fields {
        status Text
    }

    operations {
        update updateStatus(id) with (status)
    }
}