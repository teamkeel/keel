model Profile {
    fields {
        status Text
    }

    actions {
        update updateStatus(id) with (status)
    }
}

model Order {
    fields {
        status Text
    }

    actions {
        update updateStatus(id) with (status)
    }
}