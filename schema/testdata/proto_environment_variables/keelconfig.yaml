environment:
  default:
    - name: "TEST"
      value: "test"
      required:
        - "staging"
        - "production"
  staging:
    - name: "TEST2"
      value: "test2"
      required:
        - "production"

secrets:
  - name: API_KEY
    required:
      - "production"
