model MyModel {
    fields {
        nonUnique Text
        code Text @unique
    }

    operations {
        delete deleteMyModel() {
            @where(myModel.nonUnique == "something")
            @where(myModel.code == "sdsd")
        }
    }
}